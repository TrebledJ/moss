{
    "common": {
        "headers": {
            "x-data": "${state:finalIndex} -> ${state:filename} + ${state:currentIndex} :: ${state:checksum}"
        }
    },
    "cycle": [
        {
            "count": [
                1,
                1
            ],
            "delay": [
                100,
                100
            ],
            "req": [
                {
                    "method": "POST",
                    "url": "/chunkymonkey",
                    "headers": {
                    },
                    "body": "${b64:100000:100000}"
                }
            ]
        }
    ]
}